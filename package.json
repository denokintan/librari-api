{
  "name": "library-api",
  "version": "1.0.0",
  "description": "Library Loan Management API with Neon Postgres",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "setup-db": "node scripts/setup-db.js",
    "seed-db": "node scripts/seed-db.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
